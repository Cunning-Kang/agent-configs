{
  "_comment": "Template for Claude Code MCP wiring. Copy to mcp.json and fill in. Do not commit a live mcp.json. The server entry below mirrors the codebase-memory-mcp wiring observed in the local runtime; the command path is a placeholder and must be replaced with the absolute path of the binary on the target machine. Add additional servers as needed; transport may be stdio, sse, or http with corresponding url/args/env.",

  "mcpServers": {
    "codebase-memory-mcp": {
      "command": "<absolute-path-to-codebase-memory-mcp-binary-on-this-machine>"
    }
  }
}
